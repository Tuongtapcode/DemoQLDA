pipeline {
    agent any
    options {
        skipDefaultCheckout(true)  // ❌ tắt checkout mặc định
    }

    stages {
        stage('Hello') {
            steps {
                echo '✅ Jenkins pipeline is working!'
                sh 'ls -la'
            }
        }
    }
}
