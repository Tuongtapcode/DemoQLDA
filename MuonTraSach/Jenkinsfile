pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Checkout repo GitHub
                git branch: 'main', url: 'https://github.com/Tuongtapcode/DemoQLDA.git'
            }
        }

        stage('Build') {
            steps {
                echo "🚀 Repo đã checkout thành công, giờ có thể build."
            }
        }

        stage('Test') {
            steps {
                echo "🧪 Chạy test (bổ sung sau)."
            }
        }
    }

    post {
        success {
            echo '✅ Pipeline chạy thành công.'
        }
        failure {
            echo '❌ Pipeline thất bại!'
        }
    }
}
